## Objetivo
Finalizar a troca do textarea "Endpoint URLs" do provedor Modal por uma UI nativa com linhas e botão "+" (já implementada e testada no navegador), removendo o scaffolding temporário e revalidando o projeto.

## O que já está pronto (não precisa refazer)
- `src/shared/components/EndpointUrlsInput.js` (novo): uma linha por endpoint usando o `Input` nativo do app, botão ✕ por linha (desabilitado com 1 linha), botão tracejado "+ Add endpoint" (mesmo padrão do "Add Model" existente no app), hint e estado de erro.
- `AddApiKeyModal.js` e `EditConnectionModal.js` usam o novo componente (textarea e caixa com borda removidos); `src/shared/components/index.js` exporta o componente.
- Comportamento validado no navegador com cliques reais: "+" adiciona linha e foca automaticamente, Enter na última linha adiciona outra, ✕ remove e fica desabilitado quando resta 1 linha, valores preservados, botão Salvar desabilitado até ter nome + token + ≥1 URL.

## Passos restantes
1. Remover o scaffolding temporário criado para a verificação visual:
   - `src/app/preview-endpoints/` (page.js, edit/page.js, sanity/page.js)
   - `.dev-preview.log`, `.dev-preview2.log`, `.preview.html`
2. Rodar ESLint nos arquivos alterados (`EndpointUrlsInput.js`, `components/index.js`, `AddApiKeyModal.js`, `EditConnectionModal.js`). Esperado: nenhum problema novo — o erro `react-hooks/set-state-in-effect` do `EditConnectionModal` já existia antes destas mudanças.
3. Rodar os testes que cobrem o provedor e as rotas tocadas: `modal-provider`, `provider-model-discovery`, `provider-test-models-routing`, `provider-validation`, `bulk-add-names` (50+ testes).
4. Rodar `npm run build` para confirmar que o novo componente cliente (que importa `open-sse/services/modalModels.js`) compila no bundle de produção.
5. Reportar o resultado e, se você preferir, trocar o botão tracejado por um "+" compacto ao lado da última linha — é uma mudança pequena e isolada no mesmo componente (basta dizer).

## Observações
- Nenhuma mudança de comportamento do provedor Modal (registry, executor, descoberta de modelos por `/v1/models`, mapeamento modelo→endpoint) foi alterada nesta etapa — apenas a UI dos modais.
- Para testar manualmente no navegador, use `http://localhost:20127` (não `127.0.0.1`): o Next bloqueia recursos de dev em origem cruzada e a página não hidrata, então os cliques não funcionam.